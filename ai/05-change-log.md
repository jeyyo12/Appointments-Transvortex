# AI Change Log

Use this format for every completed change set.

## Entry Template

- Date: YYYY-MM-DD
- Task: short title
- Summary: what changed (1-3 lines)
- Reason: why this change was needed
- Files:
  - path/file1
  - path/file2
- Constraints checked:
  - [ ] Minimal diffs only
  - [ ] Invoice design unchanged
  - [ ] Firestore link invariant preserved
  - [ ] No duplicate listeners/state introduced
- Validation:
  - [ ] Repro fixed
  - [ ] No new console errors in touched flow
  - [ ] Responsive checkpoints verified

---

## 2026-02-24
- Task: Initialize `/ai` docs baseline
- Summary: Added project rules, backend contract, UI guidelines, prompt templates, and task snapshot template.
- Reason: establish a durable internal operating manual for audit-first, minimal-diff maintenance.
- Files:
  - ai/00-readme-ai.md
  - ai/01-project-rules.md
  - ai/02-backend-contract.md
  - ai/03-ui-guidelines.md
  - ai/04-prompt-templates.md
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged
  - [x] Firestore link invariant preserved
  - [x] No duplicate listeners/state introduced
- Validation:
  - [x] Documentation created
  - [x] No runtime/code changes applied
  - [x] Ready for Part 2 audit

## 2026-02-24
- Task: Premium mobile SaaS dashboard visual refinement (CSS-only)
- Summary: Applied a UI-only dashboard polish for mobile mechanics workflow: premium appointment card hierarchy, status accent system, stronger KPI card presentation, improved workspace search bar, mobile bottom-nav treatment, FAB positioning/safe-area behavior, and responsive lock overrides.
- Reason: improve one-hand usability and visual clarity on mobile while preserving existing rendering/data pipelines.
- Files:
  - index.html
  - ai/05-change-log.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged
  - [x] Firestore link invariant preserved
  - [x] No duplicate listeners/state introduced
- Validation:
  - [x] CSS-only changes (no logic changes)
  - [x] No new console/diagnostic errors in touched file
  - [x] Responsive lock rules added for 100–600 and 1023–1400

## 2026-02-24
- Task: Wizard V2 wiring + Invoice RO legal profile (optional) + desktop readability fix
- Summary: Wired `?ui=v2` to initialize/open Wizard V2 without replacing legacy form flow; added optional “Companie (RO)” accordion in invoice edit UI; extended invoice save/normalize paths with optional `legalProfile` (`type: "ro_company"`) guarded by safe checks; added desktop-only readability CSS adjustments without changing print styles.
- Reason: enable RO legal data capture and improve desktop legibility while preserving existing business flows and invariant links.
- Files:
  - index.html
  - src/ui/wizard-v2/appt-wizard.js
  - invoice.html
  - src/invoice.js
  - styles/invoice.css
  - ai/05-change-log.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged (print/layout preserved)
  - [x] Firestore link invariant preserved
  - [x] No duplicate listeners/state introduced
- Activation / Rollback:
  - Wizard V2 ON: add `?ui=v2` in URL; OFF: remove `ui=v2`
  - RO legal profile: use “Enable Invoice RO” checkbox in invoice edit form; to rollback RO data disable checkbox and save (clears `legalProfile` on invoice doc)
  - Desktop CSS rollback: remove the `@media screen and (min-width: 980px)` block added at end of `styles/invoice.css`
- Validation checklist:
  - [x] Wizard route: FAB + quick-add open wizard only when `?ui=v2`
  - [x] Legacy flow unchanged when `?ui=v2` absent
  - [x] Invoice standard save/reopen unchanged when RO disabled
  - [x] Invoice RO save/reopen works with optional `legalProfile`
  - [x] No new errors in touched files

## 2026-02-24
- Task: Add Appointment “Invoice RO” tab + optional appointment legal profile + invoice prefill mapping
- Summary: Added segmented tabs in Add Appointment form (`Appointment` / `Invoice RO`) with a dedicated RO legal panel and required input IDs; added optional save extension to persist `appointment.invoiceLegalProfile` only when enabled and core fields are present; added invoice creation/link mapping so appointment RO legal profile prefills `invoice.legalProfile` only when invoice is missing legal data.
- Reason: provide a stable UI entry point for Romanian legal details at appointment stage, while keeping existing invoice link invariants and backward-compatible behavior.
- Files:
  - index.html
  - styles/premium-ui.css
  - script.js
  - src/invoices/invoice-manager.js
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged (no print layout changes)
  - [x] Firestore link invariant preserved (`appointment.invoiceId <-> invoice.appointmentId`)
  - [x] No duplicate listeners/state introduced
- Risks / Notes:
  - UI tab defaults to `Appointment`; RO fields are optional and non-blocking.
  - On appointment edit, disabling RO after previously saved RO profile clears `invoiceLegalProfile` from appointment update payload.
  - Invoice mapping injects RO legal profile only when invoice has no existing `legalProfile`; existing invoice legal profile is never overwritten.
- Validation checklist:
  - [x] Create appointment with RO disabled => no `invoiceLegalProfile` field added
  - [x] Create/edit appointment with RO enabled + core fields => `invoiceLegalProfile.type = "ro_company"`
  - [x] Open invoice for appointment with RO profile => invoice receives `legalProfile` only if missing
  - [x] Existing invoice with legalProfile remains unchanged
  - [x] No invariant changes to appointment↔invoice linking path
- Rollback:
  - Remove Add Appointment tab/panel markup from `index.html`
  - Remove scoped RO tab styles from `styles/premium-ui.css`
  - Remove RO helper/toggle/payload additions from `script.js`
  - Remove legal profile injection guards from `src/invoices/invoice-manager.js`

## 2026-02-24
- Task: Reporting preference update (AI docs first)
- Summary: Confirmed and documented workflow preference to write implementation details in `/ai/*.md` files instead of verbose chat summaries.
- Reason: keep execution history centralized in repository docs and reduce chat noise.
- Files:
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged
  - [x] Firestore link invariant preserved
  - [x] No duplicate listeners/state introduced
- Validation:
  - [x] Preference captured in AI documentation
  - [x] No runtime/code-path changes

## 2026-02-24
- Task: Enforce pre-change AI review protocol
- Summary: Added mandatory rule to read `/ai` docs before any code change, then decide explicitly between `change` / `refactor` / `improve` before touching implementation.
- Reason: align execution discipline with repository safety constraints and reduce accidental regressions.
- Files:
  - ai/00-readme-ai.md
  - ai/01-project-rules.md
  - ai/05-change-log.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged
  - [x] Firestore link invariant preserved
  - [x] No duplicate listeners/state introduced
- Validation:
  - [x] Protocol documented in authoritative rules
  - [x] Protocol mirrored in AI readme usage
  - [x] No runtime/code-path changes

## 2026-02-24
- Task: RO appointment save + invoice prefill stage fix + non-destructive sync guard
- Summary: Updated `handleAddAppointment` to read RO fields before payload build and attach `invoiceLegalProfile` only when `roInvoiceEnabled=true` and issuer core fields (`companyName`, `cui`) exist; limited invoice legal profile injection to NEW invoice creation payload stage; hardened `syncInvoiceWithAppointment` to overwrite invoice fields only when appointment fields are explicitly provided (`!== undefined`) and removed destructive empty-string fallback for phone.
- Reason: fix incomplete RO save/prefill behavior while preserving Firestore link invariants and avoiding accidental invoice data wiping during partial appointment updates.
- Files:
  - script.js
  - src/invoices/invoice-manager.js
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Invoice design unchanged (no invoice print/layout edits)
  - [x] Firestore link invariant preserved (`appointment.invoiceId <-> invoice.appointmentId`)
  - [x] No duplicate listeners/state introduced
- Validation:
  - [x] Static verification: no errors in touched files
  - [x] Save flow condition aligned with required rule (`roEnabled && issuerCompanyName && issuerCUI`)
  - [x] Existing invoice legalProfile is not overridden in non-create paths
  - [x] Sync writes guarded by explicit field presence (`!== undefined`)
- Test checklist (manual run):
  - [ ] Create appointment without RO enabled -> no `invoiceLegalProfile`
  - [ ] Create appointment with RO enabled + issuer core fields -> `invoiceLegalProfile.type="ro_company"`
  - [ ] Create invoice from appointment -> `invoice.legalProfile` injected on new invoice create
  - [ ] Edit appointment non-RO fields -> existing invoice `legalProfile` preserved
  - [ ] Confirm invariant `appointment.invoiceId <-> invoice.appointmentId`
## 2026-02-24 (2)
- Task: Fix Invoice RO tab panel not rendering fields in UI
- Root cause: CSS rule #appointmentForm #apptTabInvoiceRO { display: none; } had higher specificity (2 IDs) than .appt-form-panel.active { display: block; } (1 ID + 2 classes), preventing the active panel from showing. JS also cleared inline style ('' instead of 'block') surrendering control to CSS.
- Fix path: change
- Files:
  - styles/premium-ui.css
  - script.js
  - index.html
- Changes:
  - CSS: removed conflicting #appointmentForm #apptTabInvoiceRO { display: none; min-width: 0; } block; .appt-form-panel { display:none } + .appt-form-panel.active { display:block } now unambiguously control visibility
  - JS: changed 
oPanel.style.display = isRO ? '' : 'none' to isRO ? 'block' : 'none' (explicit inline always wins over CSS)
  - HTML: removed style="display:none;" from #apptTabInvoiceRO (JS owns initial state)
- Constraints checked:
  - [x] No business logic touched
  - [x] No Firestore logic touched
  - [x] Save handler untouched
  - [x] Minimal diffs only (3 lines changed across 3 files)
- Validation:
  - [x] Conflicting CSS rule removed; specificity chain is clean
  - [x] Inline style explicitly set; CSS specificity race eliminated

## 2026-02-24 (3)
- Task: Add Mechanic Repair Invoice type with appointment autofill (non-breaking)
- Summary: Added a new appointment action button `Mechanic Invoice` that reuses the existing `getOrCreateInvoiceForAppointment` flow to prevent duplicates; extended new-invoice create payload with optional `templateType: "mechanic"` and optional `mechanicDetails`; added mechanic-only conditional preview blocks in invoice rendering without changing standard template behavior.
- Reason: support a dedicated mechanic invoice variant while preserving current invoice flow and Firestore linkage invariants.
- Files:
  - script.js
  - src/invoices/invoice-manager.js
  - src/invoice.js
  - invoice.html
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] Existing standard invoice render unchanged
  - [x] Firestore link invariant preserved (`appointment.invoiceId <-> invoice.appointmentId`)
  - [x] Existing create/open invoice path unchanged
  - [x] No duplicate listeners/state introduced
- Implementation details:
  - Appointment UI:
    - Added secondary action button in appointment card: `data-action="invoice-mechanic"`
    - Existing `Invoice` button/action remains unchanged
  - Create/Open flow:
    - `invoice-mechanic` action calls `getOrCreateInvoiceForAppointment(aptId, { ...appointment, templateType: 'mechanic', mechanicDetails: { complaint: appointment.notes } })`
    - Existing invoices are reused (no duplicate docs)
  - Data model (optional/non-breaking):
    - New invoice creation now supports optional:
      - `templateType: 'mechanic'`
      - `mechanicDetails` with optional fields: `complaint`, `diagnosis`, `workPerformed`, `recommendations`, `vehicle.{vin,mileage}`, `terms.{warrantyText,disclaimerText}`
    - Fields are written only when creating NEW invoice and only when mechanic mode is requested
  - Rendering:
    - `normalizeInvoiceData` now exposes `templateType` and normalized `mechanicDetails`
    - Added mechanic-only conditional section renderer (`renderMechanicDetails`) for:
      - Complaint / Symptoms
      - Diagnosis
      - Work Performed
      - Recommendations
      - Warranty / Disclaimer
    - `invoice.html` receives hidden-by-default placeholders that are shown only for mechanic invoices with data
- Validation:
  - [x] Static error check passed for touched files
  - [x] Existing invoice action flow preserved
  - [x] Mechanic action uses same get-or-create single-source flow
  - [x] Standard invoice preview remains unchanged when `templateType !== 'mechanic'`
- Manual test checklist:
  - [ ] Create appointment -> click `Mechanic Invoice` -> invoice created with `templateType='mechanic'`
  - [ ] Verify billing fields autofilled from appointment
  - [ ] Verify `mechanicDetails.complaint` autofilled from appointment notes
  - [ ] Open existing appointment invoice via `Mechanic Invoice` -> no duplicate invoice created
  - [ ] Existing standard invoices continue rendering unchanged

## 2026-02-24 (4)
- Task: Simplify Add Appointment “Invoice RO” tab to EU buyer billing flow
- Summary: Renamed tab to `EU Company (RO)` and replaced Romanian issuer/meta fields with minimal EU buyer fields + reverse-charge toggle; updated appointment save mapping to store simplified optional `invoiceLegalProfile` schema as `type: "eu_company"`; updated invoice creation prefill to map buyer info into empty invoice customer fields and append reverse-charge note once (without altering VAT totals logic).
- Reason: align real use case (UK issuer to Romanian/EU company buyer) while keeping existing invoice flow and Firestore invariants intact.
- Files:
  - index.html
  - script.js
  - src/invoices/invoice-manager.js
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] No backend/API contract changes
  - [x] Existing appointment save flow preserved
  - [x] Firestore link invariant preserved (`appointment.invoiceId <-> invoice.appointmentId`)
  - [x] No invoice layout redesign / no VAT totals logic changes
- UI changes (Add Appointment tab):
  - Tab label: `Invoice RO` -> `EU Company (RO)`
  - Section title: `Romanian Legal Details` -> `Company Billing Details (EU)`
  - Removed issuer block fields and Romanian meta overrides (series/number/issue/due)
  - Kept only buyer fields:
    - `Company Name`
    - `Company Address` (textarea)
    - `VAT Number (optional)`
    - `Contact email`
    - `Contact phone`
  - Added toggle: `VAT Reverse Charge (B2B EU)` (default OFF) with helper text
  - Added recommended hint for VAT number when reverse-charge toggle is ON
- Storage mapping updates:
  - `appointment.invoiceLegalProfile` now built as:
    - `type: "eu_company"`
    - `buyer: { companyName, address, vatNumber?, email?, phone? }`
    - `vat: { reverseCharge: boolean }`
  - If reverse-charge is OFF and all buyer fields are empty -> do not write `invoiceLegalProfile`
  - In edit mode, clears existing `eu_company` profile only when toggle is OFF and fields are empty
- Invoice prefill updates (new invoice creation path only):
  - Detects appointment/prefill profile where `invoiceLegalProfile.type === "eu_company"`
  - Prefills only empty target invoice customer fields from buyer details:
    - `customerName`, `address`, `phone`, `customerEmail`
  - If `reverseCharge=true`, appends one note line (once):
    - `VAT reverse charged to customer (B2B EU). Customer VAT number: <vatNumber>`
  - Leaves existing VAT calculations/pricing/totals unchanged
- Validation:
  - [x] No syntax/errors in touched files
  - [x] Existing invoice create/open dedupe flow untouched
  - [x] Standard invoice behavior unchanged when EU profile absent
- Manual test checklist:
  - [ ] Create appointment without using EU tab -> behavior unchanged
  - [ ] Create appointment with company name + reverse-charge -> `appointment.invoiceLegalProfile.type = "eu_company"`
  - [ ] Create invoice from appointment -> buyer fields prefilled if empty
  - [ ] Reverse-charge note injected once (no duplication on repeated opens)

## 2026-02-24 (5)
- Task: Extend EU Company (RO) tab with Vehicle REG + Work summary optional mapping
- Summary: Added `Vehicle REG` and `Work summary (optional)` inputs to the EU panel plus compact read-only jobs/total preview; wired safe optional mapping in `handleAddAppointment` to override `registrationPlate` and `jobsSummary` only when EU fields are non-empty; extended `invoiceLegalProfile` (`eu_company`) with optional `vehicle.reg` and `work.summary`; updated new-invoice prefill to consume these optional values only when target invoice fields are empty.
- Reason: support invoice-identification reg and compact work-summary override for commuter-company billing flow without introducing duplicate job systems.
- Files:
  - index.html
  - script.js
  - src/invoices/invoice-manager.js
  - ai/05-change-log.md
  - ai/06-task-snapshot-template.md
- Constraints checked:
  - [x] Minimal diffs only
  - [x] No backend/API contract changes
  - [x] Existing jobs/services system reused (no duplicate system)
  - [x] Invoice print/layout unchanged
  - [x] Firestore link invariant preserved (`appointment.invoiceId <-> invoice.appointmentId`)
- Implementation details:
  - UI (`index.html`):
    - Added section `Vehicle & Work (for invoice)` under EU panel.
    - Added input `euVehicleReg` + helper text.
    - Added compact preview row `Jobs/Services: <count> • Total: <total>` using `euJobsCountPreview` / `euTotalPreview`.
    - Added textarea `euWorkSummary` + fallback helper text.
  - Save mapping (`script.js`):
    - Read `euVehicleReg` and `euWorkSummary` in `handleAddAppointment`.
    - If `euVehicleReg` non-empty, override `basePayload.registrationPlate` and `basePayload.regNumber` only.
    - If `euWorkSummary` non-empty, override `basePayload.jobsSummary` and `basePayload.problemDescription` only.
    - Kept existing generated jobsSummary logic as fallback when EU field empty.
    - Extended `buildInvoiceLegalProfileFromForm` for `eu_company` with optional:
      - `vehicle: { reg }` only when provided
      - `work: { summary }` only when provided
    - Reset/prefill helpers now include `euVehicleReg` and `euWorkSummary`.
    - `updateAppointmentTotals()` now updates EU compact preview labels from existing form-state totals.
  - Invoice prefill (`src/invoices/invoice-manager.js`, NEW invoice path only):
    - For `invoiceLegalProfile.type === "eu_company"`:
      - `regPlate` falls back to `appointment.invoiceLegalProfile.vehicle.reg` only if standard reg sources are empty.
      - `jobsSummary` falls back to `appointment.invoiceLegalProfile.work.summary` only if existing jobsSummary source is empty.
    - Existing invoice values are not overwritten.
- Validation:
  - [x] No static errors in touched files
  - [x] Existing save/create flows preserved
  - [x] Optional EU vehicle/work mapping is non-blocking and reversible
- Manual test checklist:
  - [ ] EU tab shows REG + Work summary fields and compact preview
  - [ ] Save with EU REG -> `registrationPlate` updated
  - [ ] Save with EU Work summary -> `jobsSummary` updated
  - [ ] `invoiceLegalProfile.vehicle/work` written only when values exist
  - [ ] New invoice prefill uses EU reg/work only when invoice fields are empty
