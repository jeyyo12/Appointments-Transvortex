# ==========================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==========================================
# INSTRUCTION: Copy to .env file and fill with real values
# ⚠️  CRITICAL: NEVER COMMIT .env file (has secrets)
# ✅ Only .env.example is safe to commit (no real secrets here)
# ==========================================

# ==========================================
# NODE & SERVER SETUP
# ==========================================
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# ==========================================
# DATABASE (PostgreSQL)
# ==========================================
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:your_password@localhost:5432/facebook_pages_manager

# ==========================================
# JWT AUTHENTICATION (REQUIRED FOR BACKEND)
# ==========================================
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_minimum_32_characters_long_random_string
JWT_REFRESH_SECRET=your_refresh_secret_minimum_32_characters_long_random_string
JWT_EXPIRY=24h
JWT_REFRESH_EXPIRY=7d

# ==========================================
# CORS & FRONTEND URL
# ==========================================
# Comma-separated list of allowed origins
FRONTEND_URL=http://localhost:3000
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com

# ==========================================
# LOGGING
# ==========================================
LOG_LEVEL=info

# ==========================================
# RATE LIMITING (Protection against attacks)
# ==========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ==========================================
# FACEBOOK API (Optional - if integrating)
# ==========================================
# FACEBOOK_APP_ID=your_app_id_here
# FACEBOOK_APP_SECRET=your_app_secret_here

# ==========================================
# SECURITY & MONITORING (Optional)
# ==========================================
# SENTRY_DSN=https://key@sentry.io/project-id

