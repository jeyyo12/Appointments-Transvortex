/**
 * Appointments Module - Basic Placeholder
 * NOTE: This is a placeholder. Full appointments functionality
 * remains in script.js for now to minimize breaking changes.
 * 
 * TODO: Extract appointments service, UI, and events into separate files
 */

import { createLogger } from '../shared/logger.js';

const logger = createLogger('Appointments');

/**
 * Initialize appointments section (placeholder)
 * Actual implementation remains in main script.js for now
 */
export function initAppointments() {
  logger.info('Appointments module placeholder - using legacy code');
}

// Export placeholder functions
export function startAppointmentsListener() {
  logger.warn('startAppointmentsListener called on placeholder - check legacy code');
}

export function stopAppointmentsListener() {
  logger.warn('stopAppointmentsListener called on placeholder - check legacy code');
}
